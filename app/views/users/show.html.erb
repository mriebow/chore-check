
<div class="profile-info">
  <% if current_user.admin? === true %>
    <h1><%= @user.username %>'s Profile (Parent) </h1>
  <%else%>
    <h1><%= @user.username %>'s Profile</h1>
  <%end%>

  <h3><%= @user.email %></h3>

    <%= link_to 'Edit Your Profile', edit_user_registration_path %>


</div>
